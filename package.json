{
    "name": "scoped-sort",
    "displayName": "Scoped Sort",
    "description": "Sort text in a scope (vs line by line).",
    "version": "0.8.0",
    "publisher": "karizma",
    "icon": "assets/scoped-sort-red-circle.png",
    "main": "./src/extension.js",
    "repository": {
        "type": "git",
        "url": "https://github.com/karizma/vscode-scoped-sort"
    },
    "bugs": {
        "url": "https://github.com/karizma/vscode-scoped-sort/issues"
    },
    "engines": {
        "vscode": "^1.57.0"
    },
    "categories": [
        "Other",
        "Formatters"
    ],
    "keywords": [
        "sort",
        "markdown",
        "list",
        "nested",
        "recursive",
        "scoped"
    ],
    "activationEvents": [
        "onCommand:scoped-sort.sort"
    ],
    "contributes": {
        "configuration": {
            "title": "scoped-sort",
            "properties": {
                "scoped-sort.prompt": {
                    "type": "boolean",
                    "default": true,
                    "description": "Decides if the program should always prompt/ask for options"
                },
                "scoped-sort.defaultArgs": {
                    "type": "string",
                    "default": "",
                    "description": "String of arguments which follow the prompt's rules."
                }
            }
        },
        "commands": [
            {
                "command": "scoped-sort.sort",
                "title": "Scoped Sort"
            }
        ]
    },
    "scripts": {
        "test": "tape ./test/api/**.js",
        "pretty": "npx prettier --write ."
    },
    "devDependencies": {
        "@types/glob": "^7.1.3",
        "@types/node": "14.x",
        "@types/tape": "^4.13.0",
        "@types/vscode": "^1.57.0",
        "glob": "^7.1.7",
        "jest-diff": "^27.0.6",
        "prettier": "^2.3.2",
        "tape": "^5.2.2",
        "vscode-test": "^1.5.2"
    }
}
